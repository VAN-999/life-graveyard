server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/graveyard?useSSL=false&serverTimezone=UTC
    username: root
    password: 20061016
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    org.springframework: INFO